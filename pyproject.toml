[project]
name = "krep"
version = "0.1.0"
description = "Krep is Enterprise Level Agent Runtime"
readme = "README.md"
authors = [
    { name = "sedhha", email = "activity.schoolsh2@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
krep = "krep:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
